<p:datePicker
	id="{{id}}"
	{{#if required}}required="true"{{/if}}
	{{#if disabled}}disabled="true"{{/if}}
	{{#if readonly}}readonly="true"{{/if}}
	{{#if format}}pattern="{{format}}"{{/if}}
	{{#if placeholder}}placeholder="{{placeholder}}"{{/if}}
	{{#if clearable}}showButtonBar="true"{{/if}}
	/>
