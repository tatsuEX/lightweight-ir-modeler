# 開発環境 overlay（Spring の application-dev.yml 相当）
# APP_PROFILE=dev のとき application.yml へ merge される

app:
  name: lightweight-ir-modeler
  io:
    # importDir: ./data/import
    exportDir: ./data/export

# IR に関する設定
# =================================================================================================
ir:

  # IR snapshot ファイルの自動保存
  autoSave:
    enabled: true
    delay: 500
    dir: ./data/ir
    maxGenerations: 10
