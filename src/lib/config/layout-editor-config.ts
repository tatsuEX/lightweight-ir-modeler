/**
 * Property 属性エディタ（クライアント共有）向けの既定値
 *
 * WARN: server 専用の application-config から import せず、ここを参照する。
 */

/** layoutEditor.property.itemDelimiter の既定値（`${value}${delimiter}${label}`） */
export const DEFAULT_ITEM_DELIMITER = '|';
