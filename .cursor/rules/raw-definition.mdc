---
description: RawDefinition and schema validation at the trust boundary
globs: src/lib/raw/**/*,src/lib/schema/**/*
alwaysApply: false
---

# Raw Definition & Schema

- `RawDefinition` is a **temporary** model for import/export flexibility — not the SSOT (Single Source Of Truth).
- Raw must not import Reader, Writer, or Svelte/UI code.
- `SchemaValidator` is the trust boundary with the outside world.
- Validate Raw with JSON Schema/Zod (`RawDefinitionSchema`) at the import/export boundary.
- A generic IR / domain validation *engine* is deferred (see `.cursor/rules/03-out-of-scope.mdc`). A local IR check is fine when a concrete bug needs it.
- Writer-filters run **after** this Raw validation. They must not skip it.

```typescript
// ✅ GOOD — validate at boundary before transform/write
schemaValidator.validateRaw(raw);

// ❌ BAD — skip validation because "we just edited it in memory"
writer.write(raw);
```
